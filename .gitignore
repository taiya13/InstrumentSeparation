# Python
.venv/
__pycache__/
*.pyc
.ipynb_checkpoints/

# Large / downloaded artifacts (regenerate with scripts/run_poc.sh)
separated/
checkpoints/
*.ckpt
*.pth
*.onnx
# Inference audio outputs are reproducible; keep only the eval reports (json/md).
results/infer_*/*.wav

# Synthetic corpus audio is reproducible via src/make_corpus.py; keep manifests+splits.
data/synth_corpus/**/*.wav

# Keep: data/synth_orchestra (small, reproducible bench) and results/ (metrics + listenable examples)
# are intentionally committed as Phase 1 deliverables.
